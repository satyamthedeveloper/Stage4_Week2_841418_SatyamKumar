spring:
    application:
        name: zuul
    cloud:
        config:
            fail-fast: true
            retry:
                maxAttempts: 20
                maxInterval: 10000
            uri: http://config-server:8888
